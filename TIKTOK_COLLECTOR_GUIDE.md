# 📱 TikTok коллектор - Руководство

## ✅ Что создано

TikTok коллектор (`data_collectors/tiktok_collector.py`) готов к использованию!

### Возможности:
- ✅ Сбор постов из TikTok по хештегам
- ✅ Использует Apify актор "bebity/tiktok-scraper"
- ✅ Асинхронная обработка
- ✅ Обработка ошибок
- ✅ Rate limiting (задержки между запросами)
- ✅ Нормализация данных в единый формат

---

## 🔑 Требования

### 1. Apify API ключ

TikTok коллектор требует Apify API ключ:

1. **Зарегистрируйтесь на Apify:**
   - Идите на: https://apify.com
   - Создайте аккаунт (бесплатно)

2. **Получите API ключ:**
   - Перейдите в: https://console.apify.com/account/integrations
   - Скопируйте API Token

3. **Добавьте в .env:**
   ```bash
   APIFY_API_KEY=ваш_apify_api_key_здесь
   ```

### 2. Бесплатный кредит Apify

- Apify дает **$5 бесплатного кредита** при регистрации
- Один запуск TikTok актора стоит ~$0.01-0.05
- $5 хватит на **100-500 запусков**

---

## 🚀 Как использовать

### Вариант 1: Через дашборд

1. **Добавьте Apify API ключ в .env**
2. **Откройте дашборд:** `streamlit run dashboard.py`
3. **Выберите тип бизнеса** (coffee, restaurant, etc.)
4. **Нажмите "🚀 Запустить сбор данных"**
5. **TikTok коллектор автоматически соберет данные!**

### Вариант 2: Через main.py

```bash
python main.py
```

TikTok коллектор автоматически включен в пайплайн.

### Вариант 3: Тестирование отдельно

```python
import asyncio
from data_collectors import TikTokCollector

async def test():
    collector = TikTokCollector()
    data = await collector.collect(vertical="coffee")
    print(f"Собрано: {len(data)} постов")
    for item in data[:3]:
        print(f"- {item['content'][:50]}... ({item['views']} просмотров)")

asyncio.run(test())
```

---

## 📊 Что собирается

TikTok коллектор собирает:

- **post_id** - уникальный ID поста
- **content** - текст поста/описание
- **url** - ссылка на видео
- **views** - количество просмотров
- **likes** - количество лайков
- **comments** - количество комментариев
- **shares** - количество репостов
- **posted_at** - дата публикации

---

## ⚙️ Настройки

В файле `tiktok_collector.py` можно изменить:

```python
self.max_posts_per_keyword = 20  # Сколько постов собирать на ключевое слово
```

**Рекомендации:**
- Для тестирования: 10-20 постов
- Для продакшена: 50-100 постов
- Больше постов = больше API запросов = больше стоимость

---

## ⚠️ Возможные проблемы

### Проблема 1: "Invalid API key"

**Решение:**
- Проверьте, что APIFY_API_KEY правильно добавлен в .env
- Убедитесь, что ключ начинается с `apify_api_`
- Перезапустите дашборд/скрипт

### Проблема 2: "Insufficient credits"

**Решение:**
- Пополните баланс на Apify: https://console.apify.com/account/billing
- Или используйте только Google Trends (не требует ключей)

### Проблема 3: "Actor not found"

**Решение:**
- Актор "bebity/tiktok-scraper" может быть недоступен
- Проверьте на Apify: https://apify.com/bebity/tiktok-scraper
- Если недоступен, нужно найти альтернативный актор

### Проблема 4: Медленный сбор данных

**Причина:** TikTok может ограничивать частоту запросов

**Решение:**
- Это нормально, сбор может занять 1-2 минуты
- Уменьшите `max_posts_per_keyword` для ускорения

---

## 💰 Стоимость

### Примерная стоимость:

- **Один запуск актора:** $0.01-0.05
- **Сбор 3 ключевых слов:** $0.03-0.15
- **Один полный пайплайн:** $0.05-0.20

### Экономия:

1. **Ограничьте количество постов:**
   ```python
   self.max_posts_per_keyword = 10  # Вместо 20
   ```

2. **Используйте меньше ключевых слов:**
   - По умолчанию берется 3 ключевых слова
   - Можно изменить в коде

3. **Кэшируйте результаты:**
   - Сохраняйте данные в БД
   - Не собирайте повторно те же данные

---

## 🎯 Что дальше

После добавления TikTok коллектора:

1. ✅ **Больше данных** - теперь собирается с 2 платформ
2. ✅ **Лучшие тренды** - TikTok часто показывает вирусные тренды раньше
3. ✅ **Больше метрик** - просмотры, лайки, комментарии из TikTok

### Следующие шаги:

- Добавить Instagram коллектор (тоже через Apify)
- Добавить Reddit коллектор (через PRAW, бесплатно)
- Добавить YouTube коллектор (через YouTube API)

---

## 📝 Пример использования

```python
from data_collectors import TikTokCollector
import asyncio

async def main():
    collector = TikTokCollector()
    
    # Собираем данные для кофейни
    data = await collector.collect(vertical="coffee")
    
    # Выводим топ-3 по просмотрам
    top_3 = sorted(data, key=lambda x: x['views'], reverse=True)[:3]
    
    for i, post in enumerate(top_3, 1):
        print(f"{i}. {post['content'][:50]}...")
        print(f"   Просмотры: {post['views']:,}")
        print(f"   Лайки: {post['likes']:,}")
        print()

asyncio.run(main())
```

---

**Готово! TikTok коллектор работает!** 🎉

Теперь ваш проект собирает данные из Google Trends И TikTok!

