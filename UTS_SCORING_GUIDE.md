# 📊 UTS Scoring алгоритм - Руководство

## ✅ Что создано

UTS (Universal Trend Score) алгоритм (`analyzers/trend_scorer.py`) готов!

### Возможности:
- ✅ Оценка вирусности трендов по формуле UTS
- ✅ Расчет всех компонентов (Velocity, Momentum, Engagement и т.д.)
- ✅ Автоматический выбор топ-3 трендов
- ✅ Интеграция в пайплайн и дашборд

---

## 📐 Формула UTS

```
UTS = (V × M × E × G × P) / (T × C)
```

### Компоненты:

**Числитель (увеличивают score):**
- **V = Velocity** - скорость роста просмотров
- **M = Momentum** - поддержание интереса (плотность постов)
- **E = Engagement** - вовлеченность (комментарии, лайки, репосты)
- **G = Geographic** - географический охват (прокси через платформы)
- **P = Platform diversity** - разнообразие платформ

**Знаменатель (уменьшают score):**
- **T = Time decay** - временное затухание (старые тренды теряют актуальность)
- **C = Competition** - конкуренция (больше похожего контента = ниже score)

---

## 🎯 Как это работает

### 1. Подготовка данных
Собираются метрики из всех постов тренда:
- Общее количество просмотров
- Лайки, комментарии, репосты
- Платформы
- Временные метки

### 2. Расчет компонентов

**Velocity (скорость роста):**
```
V = (current_views - views_6h_ago) / 6
```
Показывает, насколько быстро растет популярность.

**Momentum (импульс):**
```
M = posts_per_day × 10
```
Показывает, поддерживается ли интерес (больше постов = выше momentum).

**Engagement (вовлеченность):**
```
E = (comments×3 + shares×5 + likes×0.1×7) / views × 100
```
Показывает качество аудитории (не просто просмотры, а взаимодействие).

**Geographic (географический охват):**
```
G = log₂(platforms + 1) / log₂(10) × 100
```
Больше платформ = больше охват (прокси для стран).

**Platform diversity (разнообразие платформ):**
```
P = platforms_count / 10 × 100
```
Больше платформ = выше score.

**Time decay (временное затухание):**
```
T = 1 + (age_hours / 24) × 0.1
```
Старые тренды теряют актуальность.

**Competition (конкуренция):**
```
C = 1 + log₁₀(posts_count + 1)
```
Больше похожего контента = больше конкуренция.

### 3. Применение формулы
```
UTS = (V × M × E × G × P) / (T × C)
```

### 4. Нормализация
Результат нормализуется к диапазону 0-100.

---

## 🚀 Как использовать

### Автоматически (в пайплайне):

UTS Scoring автоматически применяется после AI анализа:

```python
# В main.py или dashboard.py
trends = AIAnalyzer.extract_trends(analyzed_data)
scored_trends = TrendScorer.score_trends(trends)
top_3 = sorted(scored_trends, key=lambda x: x['uts_score'], reverse=True)[:3]
```

### Вручную:

```python
from analyzers import TrendScorer

# Тренды с данными постов
trends = [
    {
        'trend_name': 'Lavender Latte',
        'posts': [...],  # Список постов
        'platforms': ['tiktok', 'instagram'],
        ...
    }
]

# Оценить
scored = TrendScorer.score_trends(trends)

# Отсортировать
top_3 = sorted(scored, key=lambda x: x['uts_score'], reverse=True)[:3]
```

---

## 📊 Что вы увидите

### В дашборде:

1. **График топ-3 трендов** - столбчатая диаграмма с UTS Score
2. **Таблица деталей** - все метрики топ-3
3. **Компоненты UTS** - разбивка по каждому компоненту

### В консоли (main.py):

```
🔥 ТОП-3 ТРЕНДА (по UTS Score):

   1. Lavender Oat Milk Latte
      UTS Score: 85.32/100 ⭐
      Категория: drink
      Вирусный потенциал: 8/10
      ...
      Компоненты:
        - Velocity: 75.50
        - Momentum: 60.00
        - Engagement: 45.30
```

---

## ⚙️ Настройка

В файле `trend_scorer.py` можно изменить:

### Веса компонентов:
Сейчас все компоненты имеют равный вес. Можно изменить формулу:

```python
# Вместо: (V × M × E × G × P)
# Можно: (V² × M × E × G × P) - больше вес Velocity
```

### Пороги нормализации:
```python
# В _normalize_score можно изменить максимальный raw score
# Сейчас: 10000
# Можно: 50000 для более чувствительной оценки
```

---

## 💡 Примеры

### Высокий UTS Score (80-100):
- Много просмотров + быстрый рост
- Высокая вовлеченность
- Несколько платформ
- Свежий тренд

### Средний UTS Score (50-80):
- Хорошие метрики, но не вирусный
- Одна платформа
- Средняя вовлеченность

### Низкий UTS Score (0-50):
- Мало просмотров
- Низкая вовлеченность
- Старый тренд
- Много конкуренции

---

## 🎯 Что дальше

После UTS Scoring:

1. ✅ **Топ-3 тренда выбраны** - можно переходить к генерации идей
2. ✅ **Метрики готовы** - все компоненты рассчитаны
3. ✅ **Визуализация работает** - графики в дашборде

### Следующий шаг:
**Idea Generator** - генерация бизнес-идей для топ-3 трендов!

---

## 📝 Технические детали

### Адаптация под реальные данные:

Так как у нас нет исторических данных и данных о странах, алгоритм адаптирован:

- **Momentum:** Использует плотность постов вместо исторических данных
- **Geographic:** Использует количество платформ как прокси
- **Velocity:** Использует общие просмотры с оценкой роста

### Точность:

- Алгоритм работает с доступными данными
- Для более точной оценки нужны исторические данные
- Можно улучшить, добавив кэширование и отслеживание трендов во времени

---

**Готово! UTS Scoring работает!** 🎉

Теперь система может выбирать топ-3 самых вирусных тренда!

