# TrendScout - Движок обнаружения трендов

## 📚 Что это такое?

TrendScout - это система, которая каждый день в 7:00 утра находит топ-3 вирусных тренда для вашего бизнеса (кофейни, ресторана и т.д.) и отправляет их вам с готовыми бизнес-идеями.

## 🎯 Для кого этот проект?

Этот проект создан специально для **новичков в программировании**. Каждый файл содержит подробные комментарии, объясняющие что и зачем делается.

## 🚀 Быстрый старт

### Шаг 1: Установка Python

Убедитесь, что у вас установлен Python 3.11 или новее:

```bash
python --version
# Должно показать: Python 3.11.x или выше
```

Если Python не установлен, скачайте с [python.org](https://www.python.org/downloads/)

### Шаг 2: Создание виртуального окружения

Виртуальное окружение изолирует зависимости проекта от других проектов:

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Mac/Linux)
source venv/bin/activate

# Активировать (Windows)
venv\Scripts\activate
```

После активации в начале строки терминала появится `(venv)`.

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

Это установит все необходимые библиотеки (около 20-30 секунд).

### Шаг 4: Настройка переменных окружения

1. Скопируйте пример файла:
```bash
cp .env.example .env
```

2. Откройте `.env` в текстовом редакторе и заполните API ключи (см. ниже)

### Шаг 5: Получение API ключей

#### Apify (для TikTok и Instagram)
1. Зарегистрируйтесь на [apify.com](https://apify.com)
2. Перейдите в [Account Settings → Integrations](https://console.apify.com/account/integrations)
3. Скопируйте API Token в `.env` файл

#### Anthropic Claude (для AI анализа)
1. Зарегистрируйтесь на [console.anthropic.com](https://console.anthropic.com/)
2. Создайте API ключ
3. Скопируйте в `.env` файл

#### YouTube Data API
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте проект
3. Включите YouTube Data API v3
4. Создайте API ключ
5. Скопируйте в `.env` файл

#### Reddit API
1. Перейдите на [reddit.com/prefs/apps](https://www.reddit.com/prefs/apps)
2. Нажмите "create another app..."
3. Выберите "script"
4. Скопируйте Client ID и Client Secret в `.env`

### Шаг 6: Запуск (пока не работает, но структура готова!)

```bash
python main.py
```

## 📁 Структура проекта

```
trendscout-core/
├── config/              # Настройки приложения
│   ├── settings.py      # Загрузка переменных окружения
│   └── verticals.py     # Ключевые слова для разных бизнесов
│
├── data_collectors/     # Сбор данных с платформ
│   ├── base_collector.py    # Базовый класс для всех коллекторов
│   ├── tiktok_collector.py  # (будет создан)
│   └── ...
│
├── analyzers/           # Анализ данных
│   ├── data_filter.py   # Фильтрация данных
│   ├── ai_analyzer.py   # AI анализ через Claude
│   └── trend_scorer.py  # Алгоритм оценки трендов
│
├── generators/          # Генерация бизнес-идей
│   └── idea_generator.py
│
├── database/            # База данных
│   ├── models.py        # Модели данных (Trend, Post, etc.)
│   └── db.py            # Подключение к БД
│
├── scheduler/           # Планировщик задач
│   └── daily_job.py     # Задача на 7:00
│
├── utils/               # Вспомогательные функции
│   └── helpers.py
│
├── main.py              # Главный файл (точка входа)
├── requirements.txt     # Зависимости Python
└── .env                 # Ваши API ключи (не коммитьте в git!)
```

## 🎓 Обучение по шагам

### Урок 1: Понимание структуры проекта ✅
Вы уже здесь! Структура создана, базовые файлы готовы.

### Урок 2: Модели базы данных (следующий шаг)
Мы создадим модели для хранения трендов, постов и бизнес-идей.

### Урок 3: Коллекторы данных
Научимся собирать данные с TikTok, Instagram и других платформ.

### Урок 4: AI анализ
Интегрируем Claude API для анализа трендов.

### Урок 5: Алгоритм оценки
Реализуем формулу UTS для оценки вирусности трендов.

### Урок 6: Генерация идей
Создадим генератор бизнес-идей на основе трендов.

### Урок 7: Главный пайплайн
Соберем все вместе в один рабочий пайплайн.

## 💡 Полезные команды

```bash
# Активировать виртуальное окружение
source venv/bin/activate  # Mac/Linux
venv\Scripts\activate     # Windows

# Деактивировать виртуальное окружение
deactivate

# Установить зависимости
pip install -r requirements.txt

# Проверить установленные пакеты
pip list

# Запустить тесты (когда будут созданы)
pytest
```

## ❓ Часто задаваемые вопросы

**Q: Нужно ли платить за API?**
A: Большинство API имеют бесплатный тарифный план для начала. Apify дает $5 бесплатно, Claude - тоже есть бесплатный лимит.

**Q: Можно ли использовать SQLite вместо PostgreSQL?**
A: Да! Для начала SQLite проще. Просто укажите в `.env`: `DATABASE_URL=sqlite:///./trendscout.db`

**Q: Что делать, если что-то не работает?**
A: Проверьте:
1. Активировано ли виртуальное окружение?
2. Установлены ли все зависимости?
3. Правильно ли заполнен `.env` файл?
4. Есть ли интернет-соединение?

## 📞 Следующие шаги

После того как вы настроили окружение, мы продолжим с создания моделей базы данных. Готовы? Дайте знать!

---

**Примечание:** Этот проект создан для обучения. Все файлы содержат подробные комментарии на русском языке, объясняющие каждый шаг.

