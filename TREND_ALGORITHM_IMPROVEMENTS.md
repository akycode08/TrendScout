# 🔍 Улучшения алгоритма поиска трендов

## ✅ Что улучшено

### 1. **Новый модуль TrendFinder** (`analyzers/trend_finder.py`)

**Проблемы, которые решены:**

#### ❌ Раньше:
- Группировка только по точному совпадению названий
- "Lavender Latte" и "lavender latte" = разные тренды
- Нет объединения синонимов
- Нет поиска трендов без AI анализа

#### ✅ Теперь:
- **Умная группировка** - объединяет похожие названия (70%+ схожесть)
- **Нормализация** - "Lavender Latte" = "lavender latte"
- **Объединение синонимов** - "latte" и "coffee latte" = один тренд
- **Текстовый анализ** - находит тренды даже без AI

---

## 🎯 Как это работает

### Шаг 1: Извлечение трендов

**С AI анализом:**
- Использует результаты Claude API
- Группирует посты по названиям трендов
- Собирает метрики (просмотры, лайки и т.д.)

**Без AI анализа:**
- Анализирует текст постов
- Ищет паттерны: "прилагательное + напиток" (например, "lavender latte")
- Находит упоминания продуктов

### Шаг 2: Нормализация названий

```python
"Lavender Latte" → "lavender latte"
"The Coffee" → "coffee"
"  Hot  Latte  " → "hot latte"
```

### Шаг 3: Группировка похожих трендов

**Алгоритм схожести:**
- Точное совпадение = 100%
- Одно название содержит другое = 90%
- Схожесть строк (SequenceMatcher) = 0-100%

**Порог:** 70% схожести = объединяем

**Примеры:**
- "latte" и "coffee latte" → объединяются
- "lavender latte" и "lavender oat latte" → объединяются
- "latte" и "espresso" → не объединяются

### Шаг 4: Объединение метрик

При объединении трендов:
- Берется лучшее название (с наибольшим количеством постов)
- Суммируются все метрики (просмотры, лайки и т.д.)
- Объединяются платформы
- Берется максимальный вирусный потенциал

### Шаг 5: Фильтрация

- Минимальное количество постов (по умолчанию: 1)
- Релевантность для вертикали (coffee, restaurant и т.д.)

---

## 📊 Примеры работы

### Пример 1: Объединение похожих названий

**Входные данные:**
```
Пост 1: "Lavender Latte" (1000 просмотров)
Пост 2: "lavender latte" (500 просмотров)
Пост 3: "Lavender Oat Latte" (800 просмотров)
```

**Результат:**
```
Тренд: "Lavender Latte"
- Постов: 3
- Просмотров: 2300
- Платформы: [tiktok, instagram]
```

### Пример 2: Текстовый анализ (без AI)

**Входные данные:**
```
Пост: "Just tried this amazing cold brew coffee with oat milk!"
```

**Результат:**
```
Тренд: "Cold Brew"
- Найден через паттерн: "cold + brew"
- Категория: unknown (без AI)
- Вирусный потенциал: 5 (средний)
```

---

## 🔧 Настройки

### Изменить порог схожести:

```python
# В trend_finder.py
SIMILARITY_THRESHOLD = 0.7  # 70% схожести

# Более строго (объединяет только очень похожие):
SIMILARITY_THRESHOLD = 0.9  # 90% схожести

# Более мягко (объединяет больше):
SIMILARITY_THRESHOLD = 0.5  # 50% схожести
```

### Изменить минимальное количество постов:

```python
# В main.py или dashboard.py
trends = TrendFinder.filter_relevant_trends(
    trends, 
    vertical, 
    min_posts=2  # Минимум 2 поста
)
```

---

## 🎯 Результаты

### Раньше:
- 10 постов → 8-10 разных "трендов" (много дубликатов)
- Нет группировки
- Требуется AI для поиска

### Теперь:
- 10 постов → 3-5 уникальных трендов (объединены дубликаты)
- Умная группировка
- Работает с AI и без AI

---

## 🚀 Что дальше

### Можно улучшить:

1. **Синонимы:**
   - Добавить словарь синонимов (latte = coffee latte)
   - Использовать NLP для определения синонимов

2. **Категоризация:**
   - Улучшить определение категорий без AI
   - Использовать ключевые слова

3. **Временной анализ:**
   - Отслеживать рост трендов во времени
   - Определять новые vs старые тренды

4. **Географический анализ:**
   - Группировать тренды по регионам
   - Определять локальные vs глобальные тренды

---

**Готово! Алгоритм поиска трендов улучшен!** 🎉

Теперь система лучше находит и группирует тренды!

