# 🔍 Промпт для проверки проекта в Claude AI

Скопируйте текст ниже и отправьте Claude AI для проверки вашего проекта TrendScout:

---

## Промпт для Claude AI:

```
Привет! Я новичок в программировании и создаю проект TrendScout - систему для обнаружения вирусных трендов для бизнеса.

Пожалуйста, проверь мой код и дай обратную связь:

1. **Правильность кода** - есть ли ошибки, баги, проблемы?
2. **Качество кода** - соответствует ли код best practices?
3. **Обучение** - что можно улучшить и почему?
4. **Безопасность** - есть ли проблемы с безопасностью?
5. **Производительность** - можно ли что-то оптимизировать?

Структура проекта:
- config/ - настройки приложения
- data_collectors/ - сбор данных с платформ
- analyzers/ - анализ и фильтрация данных
- database/ - модели базы данных
- main.py - главный файл

Особенно проверь:
- data_collectors/google_trends_collector.py (Google Trends коллектор)
- analyzers/data_filter.py (фильтр данных)
- database/models.py (модели БД)
- config/settings.py (настройки)

Дай конкретные рекомендации с примерами кода, если нужно что-то исправить.
```

---

## Альтернативный вариант (более детальный):

```
Я создаю проект TrendScout на Python 3.11+ для обнаружения вирусных трендов.

Проверь следующие аспекты:

**1. Архитектура и структура:**
- Правильно ли организована структура проекта?
- Соответствует ли код принципам SOLID?
- Есть ли проблемы с зависимостями между модулями?

**2. Код-ревью конкретных файлов:**

Файл: data_collectors/google_trends_collector.py
- Правильно ли реализован async/await?
- Корректна ли обработка ошибок?
- Нет ли проблем с блокирующими операциями?

Файл: analyzers/data_filter.py
- Эффективны ли алгоритмы фильтрации?
- Правильно ли обрабатываются edge cases?
- Можно ли оптимизировать производительность?

Файл: database/models.py
- Правильно ли определены связи между моделями?
- Корректны ли типы данных?
- Есть ли проблемы с индексами?

Файл: config/settings.py
- Безопасно ли хранение настроек?
- Правильно ли используется Pydantic?

**3. Обучение:**
- Какие концепции я использую правильно?
- Что можно улучшить и почему?
- Какие паттерны программирования здесь применены?

**4. Потенциальные проблемы:**
- Есть ли race conditions?
- Проблемы с памятью?
- Утечки ресурсов?

Дай детальную обратную связь с примерами улучшений.
```

---

## Как использовать:

1. **Откройте Claude AI** (через браузер или приложение)
2. **Скопируйте один из промптов выше**
3. **Вставьте в чат Claude**
4. **Добавьте код файлов**, которые хотите проверить (можно скопировать содержимое файлов)
5. **Отправьте и получите обратную связь**

## Рекомендации:

### Вариант 1: Проверка всего проекта
Скопируйте содержимое ключевых файлов:
- `data_collectors/google_trends_collector.py`
- `analyzers/data_filter.py`
- `database/models.py`
- `config/settings.py`
- `main.py`

### Вариант 2: Пошаговая проверка
Проверяйте по одному файлу за раз для более детального анализа.

### Вариант 3: Специфические вопросы
Задавайте конкретные вопросы:
- "Правильно ли я использую async/await в google_trends_collector.py?"
- "Можно ли оптимизировать фильтрацию в data_filter.py?"
- "Есть ли проблемы с моделями БД?"

---

## Пример полного промпта с кодом:

```
Проверь этот код Google Trends коллектора:

[ВСТАВЬТЕ СОДЕРЖИМОЕ ФАЙЛА data_collectors/google_trends_collector.py]

Вопросы:
1. Правильно ли я использую async/await?
2. Есть ли проблемы с обработкой ошибок?
3. Можно ли что-то улучшить?
4. Объясни, как работает run_in_executor - зачем он нужен?
```

---

**Совет:** Начните с проверки одного файла, чтобы не перегружать Claude большим объемом кода сразу.

