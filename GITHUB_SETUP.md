# 📦 Загрузка проекта в GitHub

## ✅ Что уже сделано

1. ✅ Создан `.gitignore` файл
2. ✅ Инициализирован git репозиторий
3. ✅ Файлы готовы к коммиту

---

## 🚀 Шаги для загрузки в GitHub

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на: https://github.com/new
2. **Repository name:** `TrendScout` (или любое другое имя)
3. **Description:** `AI-powered trend discovery tool for restaurants and cafes`
4. **Visibility:** Выберите **Public** или **Private**
5. **НЕ** ставьте галочки:
   - ❌ Add a README file (у нас уже есть)
   - ❌ Add .gitignore (у нас уже есть)
   - ❌ Choose a license (можно добавить позже)
6. Нажмите **"Create repository"**

---

### Шаг 2: Подключите локальный репозиторий

После создания репозитория GitHub покажет инструкции. Выполните команды:

```bash
cd /Users/akyl/Desktop/TrendScout

# Добавьте все файлы (если еще не добавили)
git add .

# Создайте первый коммит
git commit -m "Initial commit: TrendScout - AI trend discovery tool"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/TrendScout.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/TrendScout.git

# Загрузите код
git branch -M main
git push -u origin main
```

---

### Шаг 3: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Вы должны увидеть все файлы проекта

---

## 🔐 Важно: Безопасность

### Файлы, которые НЕ должны попасть в GitHub:

✅ Уже в `.gitignore`:
- `.env` - API ключи (не загружаются)
- `*.db` - База данных (не загружается)
- `venv/` - Виртуальное окружение (не загружается)
- `__pycache__/` - Python кэш (не загружается)

### Проверка перед загрузкой:

```bash
# Проверьте, что .env не будет загружен
git status | grep .env

# Если .env в списке - удалите из индекса:
# git reset HEAD .env
```

---

## 📝 Создание README (опционально)

Если хотите улучшить README для GitHub, можно добавить:
- Описание проекта
- Скриншоты
- Инструкции по установке
- Примеры использования

---

## 🎯 Быстрая команда (все в одном)

```bash
cd /Users/akyl/Desktop/TrendScout
git add .
git commit -m "Initial commit: TrendScout - AI trend discovery tool"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/TrendScout.git
git push -u origin main
```

**Не забудьте заменить `YOUR_USERNAME` на ваш GitHub username!**

---

## ✅ Готово!

После выполнения команд ваш проект будет на GitHub!

**Ссылка на репозиторий:** `https://github.com/YOUR_USERNAME/TrendScout`

