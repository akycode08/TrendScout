# 📚 Руководство по обучению TrendScout

## 🎯 Что мы уже сделали?

### ✅ Шаг 1: Структура проекта

Мы создали структуру папок, которая разделяет код по функциональности:

```
trendscout-core/
├── config/          # ВСЕ настройки (API ключи, параметры)
├── data_collectors/ # Сбор данных с платформ
├── analyzers/       # Анализ данных
├── generators/      # Генерация бизнес-идей
├── database/        # Работа с базой данных
├── scheduler/       # Планировщик задач
└── utils/           # Вспомогательные функции
```

**Почему так?** Это называется "модульная архитектура". Каждая папка отвечает за свою задачу. Это как разделение обязанностей в команде - каждый делает свое дело.

### ✅ Шаг 2: Конфигурация (config/)

#### `config/settings.py`
Этот файл загружает все настройки из `.env` файла.

**Что такое .env файл?** Это файл с секретными данными (API ключи), который НЕ коммитится в git. Каждый разработчик создает свой `.env` с собственными ключами.

**Что делает Settings класс?**
- Читает переменные окружения из `.env`
- Проверяет типы данных (строка, число и т.д.)
- Предоставляет удобный доступ: `settings.apify_api_key`

**Пример использования:**
```python
from config import get_settings

settings = get_settings()
print(settings.vertical)  # "coffee"
```

#### `config/verticals.py`
Хранит ключевые слова для поиска трендов для разных типов бизнеса.

**Что такое вертикаль?** Это тип бизнеса: кофейня, ресторан, барбершоп и т.д.

**Зачем нужны ключевые слова?** Когда мы ищем тренды про кофе, мы ищем по словам: "coffee", "latte", "espresso" и т.д.

### ✅ Шаг 3: База данных (database/)

#### `database/db.py`
Настраивает подключение к базе данных.

**Что такое SQLAlchemy?** Это библиотека, которая позволяет работать с БД на Python без написания SQL-кода вручную.

**Что делает этот файл?**
- Создает "движок" (engine) - объект для подключения к БД
- Создает SessionLocal - класс для создания сессий
- Определяет Base - базовый класс для всех моделей

**Что такое сессия?** Это как "разговор" с базой данных. Вы открываете сессию, делаете операции, закрываете.

#### `database/models.py`
Определяет структуру таблиц в базе данных.

**Что такое модель?** Это описание таблицы. Модель = таблица в БД.

**У нас 3 модели:**

1. **Trend** (тренд)
   - Хранит информацию о вирусном тренде
   - Например: "Lavender Oat Milk Latte"
   - Содержит оценки (UTS score, velocity, momentum)

2. **Post** (пост)
   - Хранит информацию о посте из соцсетей
   - Связывается с трендом через `trend_id`
   - Содержит метрики: views, likes, comments

3. **BusinessIdea** (бизнес-идея)
   - Хранит сгенерированную AI идею для тренда
   - Содержит рецепт, цены, ROI прогноз, маркетинг

**Что такое relationship?** Это связь между таблицами. Например, один тренд может иметь много постов.

### ✅ Шаг 4: Базовый коллектор (data_collectors/)

#### `data_collectors/base_collector.py`
Базовый класс для всех коллекторов данных.

**Что такое абстрактный класс?** Это класс, который нельзя использовать напрямую. Он определяет "контракт" - какие методы должны быть у всех коллекторов.

**Паттерн Template Method:**
- Базовый класс определяет структуру
- Дочерние классы (TikTokCollector, InstagramCollector) реализуют детали

**Зачем normalize_data()?** Разные платформы возвращают данные в разном формате. Этот метод приводит все к единому виду.

### ✅ Шаг 5: Вспомогательные функции (utils/)

#### `utils/helpers.py`
Полезные функции, которые используются в разных местах.

**safe_int() и safe_float()** - безопасно преобразуют значения, не падая при ошибках.

## 🎓 Ключевые концепции для понимания

### 1. Асинхронное программирование (async/await)

**Что это?** Способ писать код, который может ждать (например, запрос к API) без блокировки всего приложения.

**Пример:**
```python
async def collect_data():
    # Это может занять время, но не блокирует другие задачи
    data = await api.get_data()
    return data
```

**Зачем?** Когда мы собираем данные с 5 платформ одновременно, это быстрее, чем делать это последовательно.

### 2. ORM (Object-Relational Mapping)

**Что это?** SQLAlchemy - это ORM. Вместо SQL-запросов мы работаем с Python-объектами.

**Вместо SQL:**
```sql
SELECT * FROM trends WHERE uts_score > 80;
```

**Мы пишем:**
```python
trends = db.query(Trend).filter(Trend.uts_score > 80).all()
```

### 3. Dependency Injection

**Что это?** Вместо создания объектов внутри функций, мы передаем их как параметры.

**Плохо:**
```python
def analyze():
    api = ClaudeAPI()  # Создаем внутри
    return api.analyze()
```

**Хорошо:**
```python
def analyze(api: ClaudeAPI):  # Получаем извне
    return api.analyze()
```

**Зачем?** Легче тестировать и менять реализации.

## 🚀 Что дальше?

### Следующие шаги (в порядке реализации):

1. **Реализовать коллекторы данных**
   - TikTokCollector (через Apify)
   - InstagramCollector
   - GoogleTrendsCollector
   - RedditCollector
   - YouTubeCollector

2. **Создать фильтр данных**
   - Удаление дубликатов
   - Фильтрация по вертикали
   - Нормализация формата

3. **Реализовать AI анализатор**
   - Интеграция с Claude API
   - Анализ постов
   - Извлечение трендов

4. **Создать алгоритм оценки (UTS)**
   - Формула: UTS = (V × M × E × G × P) / (T × C)
   - Расчет каждого компонента

5. **Реализовать генератор идей**
   - Генерация рецептов
   - Расчет цен и ROI
   - Создание маркетинговых материалов

6. **Собрать все в главный пайплайн**
   - main.py - оркестратор всего процесса

7. **Добавить планировщик**
   - Запуск каждый день в 7:00

## 💡 Советы для новичков

### 1. Не бойтесь ошибок
Ошибки - это нормально! Они показывают, что вы учитесь.

### 2. Читайте комментарии
В каждом файле есть подробные комментарии. Читайте их!

### 3. Экспериментируйте
Попробуйте изменить код и посмотреть, что произойдет.

### 4. Используйте print()
Для отладки выводите значения переменных:
```python
print(f"Значение: {variable}")
```

### 5. Изучайте по частям
Не пытайтесь понять все сразу. Разбирайте по одному файлу.

## 📖 Полезные ресурсы

- **Python документация:** https://docs.python.org/3/
- **SQLAlchemy:** https://docs.sqlalchemy.org/
- **Async/await:** https://docs.python.org/3/library/asyncio.html
- **Pydantic:** https://docs.pydantic.dev/

## ❓ Частые вопросы

**Q: Что делать, если не понимаю код?**
A: Разбирайте по строкам. Что делает каждая строка? Используйте print() для вывода значений.

**Q: Можно ли изменить структуру?**
A: Да, но лучше сначала понять, почему она такая. Потом можно улучшать.

**Q: Как тестировать код?**
A: Запускайте `python test_setup.py` для проверки настройки. Потом будем добавлять тесты.

---

**Готовы продолжить?** Дайте знать, когда будете готовы к следующему шагу! 🚀

