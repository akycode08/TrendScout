# 🔥 ГЛАВНЫЙ АЛГОРИТМ TRENDSCOUT

**Это основной алгоритм работы TrendScout - сердце системы обнаружения вирусных трендов**

---

## 📊 Общая схема работы главного алгоритма

```
┌─────────────────────────────────────────────────────────────┐
│                   1. СБОР ДАННЫХ (TikTok)                    │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │  TikTokCollector.collect()           │
        │  - Поиск по хештегам (coffee, latte) │
        │  - Apify актор: clockworks/tiktok-   │
        │    scraper                           │
        │  - Сбор: views, likes, comments,    │
        │    shares, content, posted_at         │
        └─────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│             2. ФИЛЬТРАЦИЯ ТРЕНДОВОГО КОНТЕНТА                │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │  ViralContentFilter                 │
        │                                      │
        │  Шаг 2.1: Релевантность для бизнеса │
        │  ├─ Ключевые слова (coffee, latte)  │
        │  ├─ Расширенные слова (recipe,      │
        │  │   tutorial, trending)            │
        │  ├─ Контекст (избегаем "near me")   │
        │  └─ AI анализ (если есть)          │
        │                                      │
        │  Шаг 2.2: Фильтрация по вирусности   │
        │  ├─ MIN_VIEWS >= 1000               │
        │  ├─ MIN_LIKES >= 50                 │
        │  ├─ Engagement rate >= 1%           │
        │  └─ Total engagement >= 100         │
        │                                      │
        │  Шаг 2.3: Расчет вирусного score     │
        │  ├─ Engagement rate =               │
        │  │   (likes + comments*2 +           │
        │  │    shares*5) / views              │
        │  ├─ Platform multiplier:              │
        │  │   TikTok = 1.5x                   │
        │  └─ Bonus: views > 100k = +20%       │
        │                                      │
        │  Шаг 2.4: Приоритизация             │
        │  └─ Сортировка по viral_score        │
        └─────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│             3. ПОИСК И ГРУППИРОВКА ТРЕНДОВ                   │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │  TrendFinder.find_trends()          │
        │                                      │
        │  Вариант A: С AI анализом           │
        │  ├─ Извлечение из ai_analysis       │
        │  ├─ item_name (название продукта)  │
        │  ├─ category (drink/food/pastry)    │
        │  └─ restaurant_applicable (bool)    │
        │                                      │
        │  Вариант B: Без AI (текстовый)      │
        │  ├─ Regex паттерны:                 │
        │  │   - "adjective + drink"          │
        │  │   - "cold/iced/hot + word"       │
        │  │   - "word + milk/cream/syrup"    │
        │  └─ Поиск в content                 │
        │                                      │
        │  Шаг 3.1: Нормализация названий     │
        │  ├─ lowercase                       │
        │  ├─ Удаление артиклей (the, a, an) │
        │  └─ Удаление лишних пробелов        │
        │                                      │
        │  Шаг 3.2: Группировка похожих        │
        │  ├─ Схожесть через SequenceMatcher  │
        │  ├─ Порог: similarity >= 0.7       │
        │  └─ Объединение метрик:            │
        │     - total_views, total_likes      │
        │     - total_comments, total_shares  │
        │     - platforms (список)            │
        │                                      │
        │  Шаг 3.3: Фильтрация релевантных     │
        │  ├─ min_posts >= 2                  │
        │  └─ Проверка категории для vertical │
        └─────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   4. UTS SCORING                             │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │  TrendScorer.calculate_uts_score()  │
        │                                      │
        │  Формула UTS:                       │
        │  UTS = (V * 0.25) + (M * 0.20) +    │
        │        (E * 0.20) + (G * 0.10) +    │
        │        (P * 0.15) + (T * 0.05) -    │
        │        (C * 0.05)                   │
        │                                      │
        │  Компоненты:                        │
        │  V = Velocity (скорость роста)     │
        │  M = Momentum (импульс)             │
        │  E = Engagement (взаимодействие)      │
        │  G = Geographic (география)          │
        │  P = Platform Diversity (платформы)  │
        │  T = Time Decay (временной спад)    │
        │  C = Competition (конкуренция)      │
        └─────────────────────────────────────┘
                          │
                          ▼
                    ТОП-3 ТРЕНДА
```

---

## 🔑 Ключевые компоненты главного алгоритма

### 1. **ViralContentFilter** - Фильтр вирусного контента

**Главная функция:**
```python
def filter_trending_content(data, vertical, min_engagement=100):
    # 1. Фильтр по релевантности
    relevant = _filter_business_relevant(data, vertical)
    
    # 2. Фильтр по вирусности
    viral = _filter_viral_content(relevant, min_engagement)
    
    # 3. Приоритизация
    return _prioritize_by_virality(viral)
```

**Критерии вирусности:**
- Минимум 1,000 просмотров
- Минимум 50 лайков
- Engagement rate >= 1%
- Вирусный score = (engagement_rate * 100 * platform_multiplier)

### 2. **TrendFinder** - Поиск трендов

**Главная функция:**
```python
def find_trends(data, use_ai_analysis=False):
    if use_ai_analysis:
        trends = _extract_from_ai_analysis(data)
    else:
        trends = _extract_from_text(data)
    
    # Группировка похожих
    return _group_similar_trends(trends)
```

**Алгоритм группировки:**
1. Нормализация названий (lowercase, удаление артиклей)
2. Вычисление схожести через `SequenceMatcher`
3. Объединение трендов с similarity >= 0.7
4. Агрегация метрик (views, likes, comments, shares)

### 3. **TikTok Collector** - Парсинг данных

**Главная функция:**
```python
def _collect_sync(keywords):
    for keyword in keywords:
        # Запуск Apify актора
        run = client.actor("clockworks/tiktok-scraper").call({
            "hashtags": [keyword],
            "resultsPerPage": max_posts_per_keyword
        })
        
        # Получение результатов
        for item in client.dataset(dataset_id).iterate_items():
            normalized = normalize_data(item)
            results.append(normalized)
```

**Нормализация данных:**
- `views` ← `playCount`
- `likes` ← `diggCount`
- `comments` ← `commentCount`
- `shares` ← `shareCount`
- `posted_at` ← `createTime` (парсинг timestamp)
- `url` ← формирование из `username` и `video_id`

---

## 📈 Формулы расчета главного алгоритма

### Вирусный Score
```
viral_score = engagement_rate * 100 * platform_multiplier

где:
engagement_rate = (likes + comments*2 + shares*5) / views
platform_multiplier = {
    'tiktok': 1.5,
    'instagram': 1.3,
    'youtube': 1.2
}
```

### Engagement Score
```
total_engagement = views + (likes * 10) + (comments * 5) + (shares * 20)
```

### Схожесть трендов
```
similarity = SequenceMatcher(None, name1, name2).ratio()

if name1 == name2: return 1.0
if name1 in name2 or name2 in name1: return 0.9
else: return SequenceMatcher.ratio()
```

---

## 🎯 Пороги фильтрации главного алгоритма

| Критерий | Значение | Описание |
|----------|----------|----------|
| MIN_VIEWS | 1,000 | Минимум просмотров |
| MIN_LIKES | 50 | Минимум лайков |
| MIN_ENGAGEMENT_RATE | 1% | Минимум engagement rate |
| MIN_POSTS | 2 | Минимум постов для тренда |
| SIMILARITY_THRESHOLD | 0.7 | Порог схожести для группировки |

---

## 🔄 Поток данных главного алгоритма

```
TikTok API (Apify)
    ↓
TikTokCollector
    ↓
Raw Data (views, likes, content, etc.)
    ↓
ViralContentFilter
    ↓
Trending Content (filtered by virality)
    ↓
TrendFinder
    ↓
Grouped Trends (unique trends)
    ↓
TrendScorer (UTS)
    ↓
Top-3 Trends (ranked by UTS score)
```

---

## 💡 Ключевые особенности главного алгоритма

1. **Двухуровневая фильтрация:**
   - Сначала вирусный контент
   - Потом группировка в тренды

2. **Умная группировка:**
   - Объединяет похожие названия
   - Агрегирует метрики

3. **Приоритизация:**
   - Вирусный score
   - Engagement score
   - Relevance score

4. **Гибкость:**
   - Работает с AI анализом и без него
   - Regex паттерны для текстового поиска

---

## 🎯 Заключение

Этот алгоритм является **главным алгоритмом TrendScout** и определяет, как система находит, анализирует и ранжирует вирусные тренды для вашего бизнеса. Все компоненты работают вместе, чтобы обеспечить максимально точный и релевантный результат.

**Файл:** `MAIN_ALGORITHM.md`  
**Статус:** ✅ Главный алгоритм проекта

